# 云原生微课程作业计划（审查修订版）

> 修订日期：2026-07-16  
> 适用对象：本科生云原生课程  
> 交付形式：可运行项目 + 实验文档 + 演示视频或现场 Demo

## 一、审查结论

原计划的选题方向是成立的，但当前版本还不能直接作为课程作业说明，主要问题如下：

| 问题 | 风险 | 修订决定 |
|---|---|---|
| 同时保留两个最终方向，没有明确主方案 | 团队无法确定实现边界，验收标准也无法统一 | 确定 Helm Chart 升级影响分析 Agent 为主方案，成本优化为备选 |
| 把竞品数量和“空白领域”当作差异化依据 | 竞品信息可能过时或不准确，不能证明项目有教学价值 | 改为强调可复现、证据化和课程范围内的完整闭环 |
| 让 LLM 直接判断 Breaking Change | 容易出现幻觉，无法稳定验收 | 由确定性规则和 diff 工具负责判断，Agent 只负责编排、解释和生成迁移建议 |
| 依赖 GitHub、Helm Repo 和真实集群 | 网络、权限、版本和环境差异会导致 Demo 失败 | P0 使用离线 Chart、Release Notes 和集群快照 Fixture；联网和实时集群作为 P1 |
| “当前 values”与“集群实际状态”概念混用 | `values.yaml` 不等于最终渲染结果或集群状态 | 明确区分用户 values、渲染 Manifest、CRD 和可选的 live snapshot |
| CRD 升级、回滚和自动修改范围过大 | 超出本科生微课程作业的实现能力，且有误操作风险 | 只生成迁移草稿和回滚清单，禁止自动升级、自动 Apply 和自动改代码 |
| 缺少测试样例、指标和评分方式 | “能生成一份报告”无法证明系统正确 | 增加带已知答案的 Fixture、结构化输出约束和量化验收指标 |
| 成本优化方案中的“僵尸资源”和“金额估算”定义不清 | PVC、ConfigMap、Ingress 是否可删除不能仅凭简单扫描判断 | 备选方案只做资源使用建议，不做自动清理和真实账单承诺 |

## 二、最终选题

### 题目

**Helm Chart 升级影响分析 Agent：面向 Kubernetes 应用升级的证据化评估工具**

### 要解决的问题

升级 Helm Chart 时，开发者需要同时查看旧版和新版 Chart、`values.schema.json`、模板、CRD、Release Notes 以及当前配置。人工逐项比对容易遗漏字段废弃、必填项增加、API 版本变化和 CRD 兼容性问题。

本项目提供一个只读 CLI 工具：输入旧版 Chart、新版 Chart、当前 values 和版本说明，输出带证据引用的升级影响报告以及迁移后的 values 草稿。

项目不承诺自动完成生产升级，也不把语言模型的结论作为唯一安全依据。

### 课程价值

学生可以在一个完整闭环中学习：

- Helm Chart 的目录结构、Values 和模板渲染
- Kubernetes Manifest 与 CRD 的版本兼容性
- JSON Schema 和 YAML 的结构化比较
- Agent 的工具调用、上下文组织和结构化输出
- LLM 幻觉控制、证据引用和只读权限边界
- 使用 Fixture、回归测试和 Demo 验证云原生工具

## 三、P0 最小可行范围

P0 必须在没有公网、没有真实云账号、没有付费模型的情况下运行。

### 输入

至少支持以下输入：

1. 旧版 Chart：本地 `.tgz` 或已解压目录
2. 新版 Chart：本地 `.tgz` 或已解压目录
3. 当前 `values.yaml`
4. 旧版本到新版本之间的 Release Notes Fixture
5. 可选的集群快照：`helm get values --all`、`helm get manifest` 和 CRD YAML

课程统一提供一个主 Chart 的两个固定版本 Fixture。主演示可以选用包含 CRD 的 Chart，例如 cert-manager；最终版本号应在课程开始时锁定，并随 Fixture 一起提交，不在运行时动态下载。

### 必须完成的分析

所有结论先由确定性分析器产生，再交给 Agent 解释：

- `Chart.yaml` 元数据和依赖变化
- `values.schema.json` 的字段新增、删除、类型变化、必填项变化和描述变化
- 当前 values 与新版 Schema 的兼容性校验
- 使用同一组 values 对旧版和新版执行 `helm template`，比较主要 Kubernetes 对象
- CRD 的版本新增、删除、served/storage 标记和 Schema 变化
- Release Notes 中与升级相关的已知变更
- 基于规则的风险分级：`LOW`、`MEDIUM`、`HIGH`、`CRITICAL`

P0 只分析课程选定的 Chart 和受限输入格式。不得把任意远程网页抓取、任意 Chart 兼容性和所有 Kubernetes API 迁移问题作为 P0 承诺。

### Agent 的职责

Agent 不是一个拥有集群权限的自动运维机器人，而是一个证据化分析助手。它应当：

1. 根据用户问题调用固定工具，收集 Schema、Manifest、CRD 和 Release Notes 差异
2. 将确定性分析结果组织成可读的升级影响说明
3. 解释“为什么当前 values 会受到影响”
4. 生成带路径、JSON Pointer、资源类型或证据编号的迁移步骤
5. 明确区分已确认风险、推测风险和无法判断的内容
6. 生成 values 修改草稿和回滚检查清单，但不执行修改

Agent 不得：

- 执行 `helm upgrade`、`kubectl apply`、删除资源或修改集群
- 生成任意 Shell 命令并自动执行
- 直接把自然语言判断转成未经校验的 Kubernetes YAML
- 使用没有证据引用的 Breaking Change 结论
- 访问数据库、读取用户 Secret 或绕过工具边界访问公网

模型不可用时，程序仍应输出确定性 diff；Agent 部分标记为 `INCOMPLETE`，不能伪装成完整报告。

## 四、系统设计

```text
用户问题 / CLI 参数
        |
        v
输入校验与 Fixture 装载
        |
        +--> Chart 元数据分析
        +--> Values Schema diff
        +--> 当前 values 兼容性检查
        +--> helm template Manifest diff
        +--> CRD diff
        +--> Release Notes 证据提取
        |
        v
结构化证据集合
        |
        v
只读 Agent：工具编排 + 风险解释 + 迁移草稿
        |
        v
严格 Schema 校验
        |
        +--> Markdown 报告
        +--> JSON 报告
        +--> values 草稿
        +--> 回滚清单
```

### 推荐模块

| 模块 | 责任 | 是否允许调用 LLM |
|---|---|---|
| `chart-loader` | 读取并校验本地 Chart | 否 |
| `schema-diff` | 比较 JSON Schema 和 values 使用情况 | 否 |
| `manifest-diff` | 调用 `helm template` 并比较对象 | 否 |
| `crd-diff` | 比较 CRD 版本和字段 Schema | 否 |
| `release-note-parser` | 将 Release Notes 切分为可引用证据 | 否 |
| `risk-engine` | 根据规则生成初始风险和证据 | 否 |
| `agent-orchestrator` | 调用工具、解释结果、生成迁移步骤 | 是 |
| `report-validator` | 校验 JSON、证据引用和路径 | 否 |

### 推荐技术栈

| 层 | 选择 |
|---|---|
| CLI | Python 3.12 + Typer 或 Node.js + Commander，二选一 |
| Chart 处理 | Helm CLI；必要时使用 YAML/JSON 解析库 |
| YAML | `ruamel.yaml` 或 `PyYAML` |
| JSON Schema | `jsonschema` 或同等库 |
| Kubernetes 客户端 | P0 可不直接连接集群；P1 使用官方客户端读取快照 |
| Agent | OpenAI-compatible API；默认提供 Mock Provider |
| 输出校验 | Pydantic 或 Zod strict Schema |
| 测试 | 单元测试 + Fixture 集成测试 |
| 运行环境 | 本地 Python/Node + Helm；可选 Kind/Minikube 用于展示渲染结果 |

P0 不同时维护多种语言和多种 Kubernetes 发行版。团队应在开工时锁定一种语言、Helm 版本和 Fixture 版本，并提交 lockfile 或运行环境说明。

## 五、CLI 与报告契约

### CLI 示例

```bash
helm-upgrade-agent analyze \
  --from fixtures/chart-old.tgz \
  --to fixtures/chart-new.tgz \
  --values fixtures/current-values.yaml \
  --release-notes fixtures/release-notes.md \
  --format markdown,json \
  --output artifacts/upgrade-report
```

默认命令只读本地文件，不执行升级。联网和 live cluster 必须由显式的 P1 开关启用。

### 报告最小字段

```json
{
  "chart": "example",
  "fromVersion": "1.0.0",
  "toVersion": "1.1.0",
  "verdict": "SAFE_WITH_ACTIONS",
  "riskLevel": "HIGH",
  "deterministicFindings": [],
  "agentSummary": "string",
  "migrationSteps": [],
  "rollbackChecklist": [],
  "unknowns": [],
  "evidence": []
}
```

每一条 Finding 至少包含：风险等级、影响对象、配置路径或资源路径、证据编号、原因、建议和置信度。Agent 生成的内容必须引用确定性分析器产生的证据编号；引用不存在的证据、输出非法 JSON 或超过大小限制时，报告判定为无效。

建议将最终状态限定为：

- `SAFE`：没有发现需要迁移的风险
- `SAFE_WITH_ACTIONS`：存在明确的低风险迁移步骤
- `REVIEW_REQUIRED`：存在高风险或无法自动判断的变化
- `INCOMPLETE`：输入、分析器或 Agent 失败，不能给出完整结论

## 六、测试与验收标准

课程至少提供四组带预期结果的 Fixture：

1. 无实质变化：应输出 `SAFE`
2. 当前使用的 values 字段被删除或重命名：应识别为高风险
3. 新版增加当前缺失的必填字段：应生成迁移动作
4. CRD/API 版本变化或 Release Notes 明确提示 Breaking Change：应输出 `REVIEW_REQUIRED`

P0 验收条件：

- 四组 Fixture 均能在同一条命令下重复运行
- 确定性分析结果在相同输入下完全一致
- 至少 80% 的预先植入风险被正确识别，且不得把无变化项大量判为 Breaking Change
- 每条 Agent Finding 都能追溯到证据编号、文件路径或 JSON Pointer
- Agent 输出非法、超限或缺少证据时会被拒绝，并标记 `INCOMPLETE`
- 默认运行不发起网络请求、不写入 Kubernetes 集群、不执行 `helm upgrade`
- 报告同时提供 Markdown 和 JSON；values 草稿可被 YAML 解析器重新读取
- 文档能够让助教在新环境中完成安装、运行、查看报告和复现实验

## 七、课程交付物

学生提交：

- 源代码和依赖锁定文件
- 四组以上测试 Fixture
- 一键运行脚本或明确的安装命令
- 实验文档：环境准备、输入格式、运行步骤、输出解读和故障排查
- 架构图和 Agent 工具调用说明
- 一份示例升级报告
- 3 至 5 分钟 Demo：输入升级任务、展示证据、查看 Agent 报告和 values 草稿
- 项目限制与安全说明：明确工具只读、不能替代生产升级审批

## 八、建议评分标准

| 维度 | 分值 | 重点 |
|---|---:|---|
| 核心功能 | 30 | Chart、values、Manifest、CRD 或 Release Notes 分析闭环 |
| 分析正确性 | 25 | 规则清晰、Fixture 通过、结果可重复 |
| Agent 设计 | 20 | 工具边界、证据引用、结构化输出和失败处理 |
| 云原生理解 | 15 | Helm、Kubernetes 对象、CRD 和升级风险解释准确 |
| 工程交付 | 10 | 文档、测试、可运行性和 Demo 完整 |

评分不以“调用了哪个模型”作为主要依据。没有证据的流畅文字不能替代正确的 diff、规则和可复现实验。

## 九、分阶段实施计划

### 第 1 阶段：冻结范围与 Fixture

- 确定一个 Chart 和两个版本
- 收集并提交 Chart、Release Notes、values 和预期结果
- 完成 CLI 参数、输出 Schema 和目录结构
- 验证本机 Helm 版本与渲染命令

### 第 2 阶段：完成确定性分析

- 完成 Chart 元数据、Schema、Manifest 和 CRD diff
- 完成风险规则和证据编号
- 为四组 Fixture 编写回归测试

### 第 3 阶段：接入 Agent

- 将分析器封装为只读工具
- 实现 Mock Provider 和可选真实模型 Provider
- 实现证据引用、结构化输出和失败降级
- 生成 Markdown、JSON、values 草稿和回滚清单

### 第 4 阶段：验收与演示

- 在无网络环境重复运行
- 检查 Agent 是否产生无证据结论
- 验证工具没有写集群或执行升级命令
- 完成实验文档和 Demo 脚本

## 十、成本优化方向的保留方案

如果课程更看重可视化演示，可以选择成本优化方向，但必须改名并收缩为：

> **Kubernetes 资源配置建议 Agent：基于 Prometheus 快照解释资源浪费并生成修改草稿**

备选方案 P0 只做：

- 使用 Prometheus Fixture 或固定时间窗口数据
- 分析 Deployment 的 requests/limits 与实际使用量
- 使用配置文件中的示例单价计算估算成本
- 生成带证据的降配建议和 YAML diff
- Agent 负责解释建议，不负责决定阈值和金额

备选方案 P0 不做：

- 自动删除 PVC、ConfigMap、Ingress 或其他资源
- 声称能够准确识别所有“僵尸资源”
- 接入真实云账单并对节省金额作承诺
- 一键修改或 Apply YAML
- 同时支持 Prometheus、Metrics API、Kubecost、多个云厂商和多种集群

## 十一、最终决策

本课程采用 Helm Chart 升级影响分析 Agent 作为主作业。原因是它能把 Kubernetes、Helm、Schema、CRD、版本升级和 Agent 证据化输出串成一个清晰的教学闭环，同时可以通过离线 Fixture 避免环境不稳定。

成本优化方向作为下一次课程或 P1 扩展，不再与主方案并列推进。无论选择哪一方向，确定性分析都必须负责事实判断，Agent 负责工具编排、解释和报告生成，所有涉及集群写操作的能力都不进入本课程 P0。
